name: 'Auto Releasing Draft'
description: 'Draft a GitHub release based on a newly closed milestone'
author: 'Alyssa Jumapao'
outputs:
  release-url:
    description: 'The URL of the GitHub release that was drafted'
runs:
  using: 'docker'
  image: 'Dockerfile'
